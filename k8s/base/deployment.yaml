apiVersion: apps/v1
kind: Deployment
metadata:
  name: verseminer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: verseminer
  template:
    metadata:
      labels:
        app: verseminer
    spec:
      containers:
        - name: app
          image: ghcr.io/krishnarajt/verseminer:git-4dfcbc8a5e6c8ac04357ed59a65d40235d1c1fdb
          imagePullPolicy: IfNotPresent
          command: ["python", "main.py"]

          envFrom:
            - secretRef:
                name: verseminer-env

          volumeMounts:
            - name: app-config
              mountPath: /app/config.yml
              subPath: config.yml
            
            - name: music
              mountPath: /music
            
            - name: db-data
              mountPath: /app/db_data

      volumes:
        - name: app-config
          configMap:
            name: app-config

        - name: music
          hostPath:
            path: /music/albums
            type: Directory
        
        - name: db-data
          persistentVolumeClaim:
            claimName: verseminer-db-pvc